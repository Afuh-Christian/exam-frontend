import { StyleSheet } from "react-native";
import { windowheight } from "./Components/Navbar/Navbar";


 const indexStyles = StyleSheet.create({
    body: {
        //  backgroundColor: "pink", 
        height: (windowheight - 80)
    }
})

export default indexStyles
